<!DOCTYPE html>
<html>
  <head>
    <style>
        @media print {
            @page {
                size: 6.2cm 2.9cm landscape;
            }
        }

        .label {
            margin: 8mm 2mm 2mm 2mm;
            top: 50%;
            text-align: center;
            font-family: sans-serif;
            font-size: 13pt;
            font-weight: bold;
            page-break-after: always;
        }

        .label:last-child {
            page-break-after: auto;
        }

        .label p {
            margin: 0;
        }
    </style>
  </head>
  <body>
    {% for i in 0..2 %}
    <div class="label">
      <p>{{ user.name }}</p>
      <p>{{ user.groups|formatList({'property': 'shortName', 'relation': 'ampersand'}) }}</p>
    </div>
    {% endfor %}
    <script>
      window.print();
    </script>
  </body>
</html>
