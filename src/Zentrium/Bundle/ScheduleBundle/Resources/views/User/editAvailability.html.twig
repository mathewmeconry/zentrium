{% extends 'ZentriumScheduleBundle:User:availability.html.twig' %}
{% form_theme form 'ZentriumCoreBundle::form.html.twig' %}

{% set user = availability.user %}

{% block content %}
<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title">{{ 'zentrium_schedule.user.availability.title'|trans }}</h3>
  </div>
  <div class="box-body">
    {{ form(form) }}
  </div>
</div>
{% endblock %}
